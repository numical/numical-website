{
  "name": "memory",
  "version": "1.0.0",
  "description": "",
  "main": "memory.js",
  "scripts": {
    "start": "http-server . -S -C ./ssl/server-cert.pem -K ./ssl/server-key.pem -p 8080 -o",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.11.1"
  }
}
